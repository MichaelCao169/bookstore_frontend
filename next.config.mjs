/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    unoptimized: true,
  },
  // Turbopack configuration
  turbopack: {
    // Add turbopack config here if needed
  },
};
 
export default nextConfig;
